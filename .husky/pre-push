#!/bin/sh
# Mirror CI before every push: token governance + storybook proof + static
# checks + LOC guards + full test suite. Clear Git's repository-local variables
# so fixture Git commands run against their own repository, not this worktree.
local_git_env=$(git rev-parse --local-env-vars) || exit $?
unset $local_git_env
just preflight
