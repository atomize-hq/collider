{
  "$comment": "Collider's values for the x-repo-profile extension points in ../schemas. Pass with --profile to check an artifact against Collider's vocabulary as well as the portable shape. A different repo writes its own file here and changes nothing in schemas/.",

  "artifact-path": {
    "const": "design-tokens/dist/figma/tokens.json"
  },

  "publish-modes": {
    "enum": ["plugin-import-manual", "tokens-studio-carried"],
    "description": "Collider publishes through the repo-owned Figma plugin. The Enterprise Variables REST rail was retired — seat-gated, never run to success, and its code is gone. The Tokens Studio carrier was defined and never used."
  },

  "promotion-levels": {
    "enum": [
      "A-source-valid",
      "B-projection-valid",
      "C-consumption-valid",
      "D-publish-valid",
      "E-promotion-complete"
    ]
  },

  "tier-policy": {
    "enum": ["primitive", "interactive", "workflow"],
    "description": "Mirrors storybook/component-tier-policy.json tierOrder. Keep the two in step."
  },

  "tier-names": {
    "items": { "enum": ["primitive", "interactive", "workflow"] }
  },

  "consumer-ids": {
    "items": { "type": "string", "pattern": "^THR-[0-9]{2}$" }
  },

  "generated-artifacts": {
    "description": "Collider's three generated proof surfaces: token docs, recipe docs, runtime CSS parity."
  }
}
