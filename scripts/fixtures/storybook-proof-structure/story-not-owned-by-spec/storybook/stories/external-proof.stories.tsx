import type { Meta, StoryObj } from '@storybook/nextjs-vite';

const meta = {
  title: 'Contracts/External Docs',
} satisfies Meta;

export default meta;

type Story = StoryObj<typeof meta>;

export const ExternalProof: Story = {};
