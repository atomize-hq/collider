# Minimal kickoff for a future proof-planning or implementation session

Use only after the desired task has been explicitly authorized.

```text
Unzip the attached Collider Agent Native canonical bundle and run
`python3 meta/VERIFY-BUNDLE.py` from its root. Read START-HERE.md, the
three canonical documents, the adjudication artifacts, and
proof/agentkit-owner-boundary-proof-spec.md.

Treat the proof specification as the controlling proof contract, not as
implementation authorization. Preserve Handbook/Substrate/Collider owner
boundaries and keep C-OQ-027 Open unless a separate decision explicitly
closes it.

For proof planning, first produce the exact supported owner-surface
inventory and field-by-field identity/lifecycle mapping required by Gate A.
For implementation, proceed only if the current instruction separately
authorizes the relevant proof stage. Keep fixture, owner-integrated, and
deployment/artifact evidence clearly separated.
```
