export default function Home() {
  return (
    <main className="min-h-screen" style={{ background: 'var(--color-background-base)' }}>
      <p style={{ color: 'var(--color-text-secondary)', padding: '2rem' }}>Collider</p>
    </main>
  );
}
